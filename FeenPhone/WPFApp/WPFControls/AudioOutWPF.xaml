<UserControl x:Class="FeenPhone.WPFApp.Controls.AudioOutWPF"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel Background="White">
        <TextBlock Background="Wheat" DockPanel.Dock="Top" TextAlignment="Center">AUDIO OUT</TextBlock>
        <ProgressBar DockPanel.Dock="Left" Height="{Binding ElementName=stack, Path=ActualHeight}" Width="20" Value="{Binding Path=Max}" Maximum="100" Orientation="Vertical" />
        <StackPanel Name="stack">
            <StackPanel.Resources>
                <Style TargetType="{x:Type Label}">
                    <Setter Property="Width" Value="100"/>
                    <Setter Property="Height" Value="18"/>
                    <Setter Property="VerticalAlignment" Value="Top"/>
                    <Setter Property="Padding" Value="0"/>
                </Style>
                <Style TargetType="{x:Type TextBlock}">
                    <Setter Property="Height" Value="18"></Setter>
                    <Setter Property="VerticalAlignment" Value="Top"/>
                    <Setter Property="Padding" Value="10 0 0 0"/>
                </Style>
            </StackPanel.Resources>
            <WrapPanel>
                <Label>Format</Label>
                <TextBlock Text="{Binding Path=OutputFormat}" d:DataContext="{d:DesignData Source=SampleData/MiscData.xaml}" ></TextBlock>
            </WrapPanel>
            <WrapPanel>
                <Label>Codec</Label>
                <TextBlock Text="{Binding Path=CodecName}" d:DataContext="{d:DesignData Source=SampleData/MiscData.xaml}" ></TextBlock>
            </WrapPanel>
            <DockPanel>
                <Label>Buffer</Label>
                <Grid Name="progressBarContainer">
                    <ProgressBar  d:DataContext="{d:DesignData Source=SampleData/MiscData.xaml}" Height="{Binding ElementName=progressBarContainer, Path=ActualHeight}" Width="{Binding ElementName=progressBarContainer, Path=ActualWidth}" Value="{Binding Path=BufferedDuration}"  Maximum="{Binding Path=MaxBufferedDuration}" />
                    <TextBlock Text="{Binding Path=BufferedDurationString}" d:DataContext="{d:DesignData Source=SampleData/MiscData.xaml}" ></TextBlock>
                </Grid>
            </DockPanel>
            <WrapPanel>
                <Label>MaxBufferedDuration</Label>
                <TextBlock Text="{Binding Path=MaxBufferedDuration}" d:DataContext="{d:DesignData Source=SampleData/MiscData.xaml}" ></TextBlock>
            </WrapPanel>
            <WrapPanel>
                <Label>DroppedFrames</Label>
                <TextBlock Text="{Binding Path=DroppedPackets}" d:DataContext="{d:DesignData Source=SampleData/MiscData.xaml}" ></TextBlock>
            </WrapPanel>
            <WrapPanel>
                <Label>SilenceBytes</Label>
                <TextBlock Text="{Binding Path=DroppedSilence}" d:DataContext="{d:DesignData Source=SampleData/MiscData.xaml}" ></TextBlock>
            </WrapPanel>
            <WrapPanel>
                <Label>UnderRuns</Label>
                <TextBlock Text="{Binding Path=UnderRuns}" d:DataContext="{d:DesignData Source=SampleData/MiscData.xaml}" ></TextBlock>
            </WrapPanel>
        </StackPanel>
    </DockPanel>
</UserControl>
