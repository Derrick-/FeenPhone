<UserControl x:Class="FeenPhone.WPFControls.AudioWPF"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel>
        <TextBlock Background="Wheat" DockPanel.Dock="Top" TextAlignment="Center">AUDIO</TextBlock>
        <CheckBox ClickMode="Release" DockPanel.Dock="Top" IsChecked="{Binding Path=IsRecording, Mode=TwoWay}" ToolTip="Enable sound recording">Record</CheckBox>
        <ComboBox DockPanel.Dock="Top" Name="comboBoxCodecs" IsEnabled="{Binding ControlsEnabled}" />
        <ComboBox ItemsSource="{Binding Path=InputSourceList, Mode=TwoWay}" SelectedIndex="{Binding Path=SelectedInputSourceIndex, Mode=TwoWay}" SelectedItem="{Binding Path=SelectedInputSource, Mode=TwoWay}" IsEnabled="{Binding ControlsEnabled}"/>
    </DockPanel>
</UserControl>
